const express = require("express");
const app = express.Router();
const quriesMatching = require("../mysql/quriesMatching");
const pConnection = require("../mysql/connection");

/*
add sth a seen array
add to likes table

pull all users stiching data back together
 */

module.exports = app;
